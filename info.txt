# Create instance of FPDF class again with UTF-8 handling
from fpdf import FPDF

# Create the PDF again, handling encoding issues
pdf = FPDF()

# Add a page
pdf.add_page()

# Set title
pdf.set_font("Arial", size=16, style='B')
pdf.cell(200, 10, txt="Publishing Your Django Middleware Package to PyPI", ln=True, align="C")

# Add subtitle
pdf.ln(10)  # Line break
pdf.set_font("Arial", size=12)
pdf.cell(200, 10, txt="Step-by-Step Guide", ln=True, align="C")

# Add the content
content = """
This guide explains how to publish your Django middleware package (or any Python package) to PyPI (Python Package Index).

1. **Prepare Your Package**

   Your Django middleware package should follow a standard Python package structure. Here is an example structure:

   restricted_countries/
   ├── restricted_countries/
   │   ├── __init__.py
   │   ├── middleware.py
   │   ├── utils.py
   │   └── settings.py
   ├── tests/
   │   └── test_middleware.py
   ├── MANIFEST.in
   ├── README.md
   ├── setup.py
   ├── LICENSE
   └── requirements.txt

2. **Create `setup.py`**

   The `setup.py` file tells PyPI how to install and package your project.

   ```python
   from setuptools import setup, find_packages

   setup(
       name='django-restricted-countries',
       version='0.1.0',
       description='Django middleware to restrict access based on the client\'s country',
       long_description=open('README.md').read(),
       long_description_content_type='text/markdown',
       author='Your Name',
       author_email='your.email@example.com',
       url='https://github.com/yourusername/django-restricted-countries',
       packages=find_packages(),
       classifiers=[
           'Programming Language :: Python :: 3',
           'Framework :: Django',
           'License :: OSI Approved :: MIT License',
           'Operating System :: OS Independent',
       ],
       install_requires=[
           'Django>=3.0',
           'geoip2>=4.0',
           'ipware>=2.1',
       ],
       include_package_data=True,
       python_requires='>=3.6',
   )
